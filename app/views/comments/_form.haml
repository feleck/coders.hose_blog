.row
  .col-md-12
    = form_with(model: [ @post, @post.comments.build ], local: true) do |form|
      .form-group
        = form.text_area :body, class: 'form-control'#, required: true, placeholder: 'Enter comment'
      = form.submit class: 'btn btn-sm btn-secondary'
